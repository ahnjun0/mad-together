import React from 'react';
import ReactDOM from 'react-dom/client';
import FishingRodTest from './FishingRodTest';
import '../index.css'; // Tailwind CSS

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FishingRodTest />
  </React.StrictMode>
);
