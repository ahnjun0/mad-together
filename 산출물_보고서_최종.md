# 🎣 KaHook! - 인터랙티브 멀티스크린 파티 게임

**KaHook!**은 스마트폰을 낚시대 컨트롤러로 사용하여, 대형 스크린(PC)을 보며 즐기는 **인터랙티브 멀티 스크린 파티 게임**입니다.

가속도 센서(Accelerometer)를 이용한 캐스팅과 흔들기(Shaking) 인식을 통해, 실제 낚시를 하는 듯한 손맛과 몰입감을 제공합니다. 웹 기술만으로 구현되어 앱 설치 없이 QR 코드 스캔만으로 즉시 참여할 수 있습니다.

### 🔗 **Play Live:** [https://madcamp.cloud](https://madcamp.cloud/)

---

## 👥 팀 구성

### 안준영
- **소속:** 부산대학교 정보컴퓨터공학부 22학번
- **GitHub:** [ahnjun0](https://github.com/ahnjun0)
- **역할:** Full Stack Developer

### 임태빈
- **소속:** 서울대학교 컴퓨터공학부 24학번
- **GitHub:** [imtaebin83-debug](https://github.com/imtaebin83-debug)
- **역할:** Full Stack Developer

---

## 🤔 개발 목표

> *"무엇을 만들려 했냐면…"*

- **함께 즐기는 경험**: 다 같이 즐길 수 있는 결과물을 만들어보자!
- **실제 서비스 지향**: 시연에서 그치지 않고, 실제 출시까지 이어질 수 있는 완성도 높은 결과물 제작
- **기술 실험**: 최신 웹 기술을 활용한 센서 기반 멀티스크린 인터랙션 구현

---

## 🎮 게임 컨셉 & 플레이 방법

**"당신의 폰이 낚시대가 됩니다!"**

### 게임 구성
1. **호스트 (PC 화면)**: 드넓은 3D 바다와 배가 펼쳐지는 메인 게임 화면. QR 코드를 띄워 플레이어를 모집하고 게임 상황을 실시간으로 중계합니다.

2. **플레이어 (모바일)**: QR 코드로 접속하면 스마트폰이 낚시 컨트롤러로 변신합니다.

3. **게임 액션**:
   - **Casting (던지기)**: 팀장이 스마트폰을 실제 낚시대처럼 휘둘러 찌를 멀리 던집니다 (가속도 센서 Peak 감지)
   - **Reeling (감기)**: 물고기가 잡히면 스마트폰을 빠르게 흔들어 낚아 올립니다 (Shake 모션 감지)
   - **Haptic (손맛)**: 배가 움직이거나 입질이 올 때 스마트폰 진동을 통해 현장감 전달

### 게임 플레이 흐름
1. PC에서 방 생성 → QR 코드 생성
2. 모바일로 QR 스캔 → Google 로그인 → 팀 선택
3. 모든 플레이어 준비 완료 → 게임 시작
4. 시네마틱 연출 → 센서 튜토리얼
5. 팀장 캐스팅 → 팀원 전체가 흔들기로 협력
6. 힘을 합해 Shake를 하여 먼저 물고리를 잡은 팀 승리!

---

## 🚀 핵심 기능

### 1. 멀티 디바이스 실시간 동기화
- **WebSocket(Socket.io)** 기반 PC(호스트)와 다수 모바일(클라이언트) 간 상태 실시간 동기화
- **Redis** 를 활용한 초고속 게임 세션 상태 관리 및 점수 집계
- ms 단위 레이턴시로 수십 명의 플레이어 동시 참여 지원

### 2. 실시간 모션 인식 (Sensor Fusion)
- **DeviceMotion API** 활용: 가속도계(Accelerometer) 기반 제스처 인식
- **Peak Detection**: 캐스팅 동작의 순간 최대 가속도 측정
- **Shake Detection**: Schmitt Trigger 알고리즘을 통한 흔들기 동작 필터링 및 카운팅
- 사용자 제스처(던지기 강도, 흔들기 속도)를 정밀하게 분석하여 게임에 반영

### 3. 몰입형 3D 그래픽
- **React Three Fiber (R3F)** 를 사용한 웹 기반 3D 렌더링
- 낚시대 애니메이션: SVG Path Morphing을 통한 텐션 표현
- 물고기 이동: Lerp(선형 보간)를 사용한 부드러운 움직임
- 파티클 효과, 물보라 애니메이션, 모션 블러 등 몰입감 극대화

### 4. 팀 배틀 시스템
- **A팀 vs B팀** 대항전 모드
- 팀별 리더 선정 및 위임 시스템
- 실시간 스코어링 및 개인 기여도 추적
- Redis 기반 고속 점수 집계 및 리더보드

### 5. 반응형 UX/UI
- 접속 기기에 따라 자동으로 최적화된 화면 제공
  - **PC**: 3D 메인 뷰, 팀 스플릿 화면, QR 코드 패널
  - **모바일**: 터치 최적화 컨트롤러 UI, 실시간 피드백
- **Tailwind CSS** 기반 모던 디자인 시스템
- **Framer Motion** 을 활용한 부드러운 전환 애니메이션

### 6. 재접속 및 세션 복구
- 연결 끊김 시 자동 재연결
- 게임 중 로그아웃하더라도 재로그인 시 이전 상태 복구
- Redis + PostgreSQL 하이브리드 세션 관리

---

## 🛠 기술 스택 (Tech Stack)

### Frontend (Monorepo)

#### **Web PC (`apps/web-pc`)**
- **Core Framework**: React 19, Vite 7
- **3D Engine**: 
  - React Three Fiber 9.5 (Three.js wrapper)
  - Drei 10.7 (R3F 헬퍼 라이브러리)
- **Styling**: TailwindCSS 3.4
- **State Management**: Zustand 4.5 (Immer middleware)
- **Real-time Communication**: Socket.io Client 4.8
- **Animation**: Framer Motion 11.0
- **Authentication**: @react-oauth/google

#### **Web Mobile (`apps/web-mobile`)**
- **Core Framework**: React 19, Vite 7
- **Sensors**: 
  - DeviceMotion API (가속도계, 자이로스코프)
  - Vibration API (햅틱 피드백)
- **UI Components**: Lucide React (아이콘), Clsx (조건부 스타일링)
- **State Management**: Zustand 4.5 (Immer middleware)
- **Real-time Communication**: Socket.io Client 4.8
- **Animation**: Framer Motion 11.0
- **Debugging**: vConsole (모바일 디버깅)

### Backend (`apps/server`)

- **Framework**: NestJS 11 (Node.js)
- **Database**:
  - **PostgreSQL 15**: 사용자 정보, 방 정보, 게임 결과 등 영구 데이터 저장
  - **Redis (ioredis 5.9)**: 실시간 게임 세션, 점수, 플레이어 상태 관리
- **ORM**: Prisma 7.2 (PostgreSQL adapter)
- **Real-time Communication**:
  - Socket.io 4.8 (WebSocket Gateway)
  - @nestjs/websockets, @nestjs/platform-socket.io
- **Authentication**: 
  - JWT (passport-jwt)
  - Google OAuth 2.0 (google-auth-library)
- **Utilities**: QRCode 생성, Bcrypt (암호화)

### Infrastructure & DevOps

- **Cloud Platform**: Amazon AWS EC2
- **Containerization**: 
  - Docker
  - Docker Compose (멀티 컨테이너 오케스트레이션)
- **Web Server**: Nginx (Reverse Proxy, SSL Termination, Static File Serving)
- **SSL/TLS**: Let's Encrypt (Certbot) - 자동 갱신
- **CI/CD**: Docker 기반 빌드 및 배포 자동화

---

## 🏗 시스템 아키텍처

### 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                         클라이언트 계층                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐              ┌─────────────────────┐    │
│  │   💻 PC Host      │              │  📱 Mobile Players  │    │
│  │   (Browser)       │              │    (Browser)        │    │
│  │                   │              │                     │    │
│  │ • React + R3F     │              │ • React             │    │
│  │ • 3D 렌더링       │              │ • DeviceMotion API  │    │
│  │ • Zustand Store   │              │ • Zustand Store     │    │
│  └─────────┬─────────┘              └──────────┬──────────┘    │
│            │                                   │                │
│            │ WebSocket (Socket.io)             │                │
│            │ + REST API                        │                │
└────────────┼───────────────────────────────────┼────────────────┘
             │                                   │
             ▼                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                         AWS EC2 Instance                         │
│                    (Docker Compose Environment)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    🌐 Nginx (Reverse Proxy)                │ │
│  │  • SSL Termination (Let's Encrypt)                        │ │
│  │  • Static File Serving (web-pc, web-mobile)              │ │
│  │  • WebSocket Upgrade (wss://)                            │ │
│  │  • Load Balancing & Routing                              │ │
│  └────────────┬──────────────────────────────────────────────┘ │
│               │                                                 │
│               ├─ /api/* ──────────┐                            │
│               ├─ /game (ws) ──────┤                            │
│               │                   ▼                            │
│  ┌────────────┴─────────────────────────────────────────────┐ │
│  │           ⚙️  NestJS Backend (Node.js)                    │ │
│  │                                                            │ │
│  │  ┌──────────────────┐      ┌──────────────────────────┐  │ │
│  │  │  REST API         │      │  Socket.io Gateway       │  │ │
│  │  │  • /api/auth      │      │  • /game (WebSocket)     │  │ │
│  │  │  • /api/rooms     │      │  • 실시간 이벤트 처리    │  │ │
│  │  │  • JWT Auth       │      │  • 게임 상태 동기화      │  │ │
│  │  └──────────────────┘      └──────────────────────────┘  │ │
│  │               │                        │                  │ │
│  │               └────────┬───────────────┘                  │ │
│  │                        │                                  │ │
│  │         ┌──────────────┼──────────────┐                  │ │
│  │         │              │              │                  │ │
│  │         ▼              ▼              ▼                  │ │
│  │  ┌──────────┐  ┌─────────────┐  ┌──────────┐           │ │
│  │  │ Prisma   │  │   Redis      │  │ Google   │           │ │
│  │  │  ORM     │  │   Service    │  │ OAuth    │           │ │
│  │  └────┬─────┘  └──────┬──────┘  └──────────┘           │ │
│  └───────┼────────────────┼────────────────────────────────┘ │
│          │                │                                   │
│          ▼                ▼                                   │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │ 🔵 PostgreSQL │  │ 🔴 Redis      │                         │
│  │ Container     │  │ Container     │                         │
│  │               │  │               │                         │
│  │ • 영구 데이터 │  │ • 실시간 상태 │                         │
│  │ • 사용자 정보 │  │ • 게임 세션   │                         │
│  │ • 방/게임 정보│  │ • 점수/Ready  │                         │
│  │ • 전적 기록   │  │ • 초고속 집계 │                         │
│  └──────────────┘  └──────────────┘                         │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

### 통신 플로우

#### 1. **모바일 ↔ 서버 ↔ PC 실시간 동기화**

```
[Mobile] shake 동작 감지
    ↓ (Socket.io)
[Server/Redis] 점수 증가 (HINCRBY)
    ↓ (Socket.io broadcast)
[PC] score_update 이벤트 수신 → Zustand store 업데이트 → 3D 화면 리렌더링
```

#### 2. **Redis 기반 초고속 상태 관리**

- **Key Pattern**:
  - `room:{roomId}` → 팀 점수 (Hash)
  - `room:{roomId}:player:{playerId}` → 플레이어 상태 (Hash)
  - `room:{roomId}:connected` → 연결된 플레이어 목록 (Set)

- **장점**:
  - In-memory 연산으로 ms 단위 응답 속도
  - HINCRBY를 통한 원자적 점수 증가
  - 게임 종료 시 PostgreSQL로 최종 결과만 저장

#### 3. **WebSocket 네임스페이스 분리**

- `/game` 네임스페이스로 게임 전용 이벤트 격리
- Room 기반 Broadcasting으로 효율적인 메시지 전달
- JWT 기반 인증 (socket.handshake.auth.token)

---

## 📂 프로젝트 구조

```
mad-together/
├── apps/                           # Monorepo 앱 디렉토리
│   ├── server/                     # NestJS 백엔드 서버
│   │   ├── src/
│   │   │   ├── auth/               # 인증 (JWT, Google OAuth)
│   │   │   │   ├── auth.controller.ts
│   │   │   │   ├── auth.service.ts
│   │   │   │   └── strategies/jwt.strategy.ts
│   │   │   ├── games/              # 게임 로직 및 WebSocket 게이트웨이
│   │   │   │   ├── games.gateway.ts   # Socket.io 이벤트 핸들러
│   │   │   │   └── games.service.ts
│   │   │   ├── rooms/              # 방 생성 및 관리 REST API
│   │   │   │   ├── rooms.controller.ts
│   │   │   │   └── rooms.service.ts
│   │   │   ├── redis/              # Redis 클라이언트 서비스
│   │   │   │   └── redis.service.ts
│   │   │   ├── prisma/             # Prisma ORM 설정
│   │   │   │   └── prisma.service.ts
│   │   │   ├── common/             # 공통 유틸 (guards, decorators, config)
│   │   │   └── main.ts
│   │   ├── prisma/                 # DB 스키마 및 마이그레이션
│   │   │   ├── schema.prisma
│   │   │   └── migrations/
│   │   ├── Dockerfile
│   │   └── package.json
│   │
│   ├── web-pc/                     # PC 호스트 디스플레이 웹앱
│   │   ├── src/
│   │   │   ├── views/              # 화면별 컴포넌트
│   │   │   │   ├── LoginView.jsx        # PC 로그인
│   │   │   │   ├── HomeView.jsx         # 방 생성
│   │   │   │   ├── WaitingView.jsx      # 대기실
│   │   │   │   ├── CinematicView.jsx    # 출항 연출
│   │   │   │   ├── TutorialView.jsx     # 센서 확인 & 튜토리얼
│   │   │   │   ├── CastingView.jsx      # 캐스팅 단계
│   │   │   │   ├── PlayingView.jsx      # 메인 게임
│   │   │   │   └── FinishedView.jsx     # 결과 화면
│   │   │   ├── components/         # 재사용 가능한 UI 컴포넌트
│   │   │   │   ├── FishingRod3D.jsx     # 3D 낚시대 (R3F)
│   │   │   │   ├── CastingRod3D.jsx     # 3D 캐스팅 애니메이션
│   │   │   │   ├── WaterSplash.jsx      # 물보라 효과
│   │   │   │   ├── QRCodePanel.jsx      # QR 코드 표시
│   │   │   │   ├── TeamPanel.jsx        # 팀 정보 패널
│   │   │   │   ├── PlayerCard.jsx       # 플레이어 카드
│   │   │   │   ├── SplitScreen.jsx      # 팀 분할 화면
│   │   │   │   ├── Confetti.jsx         # 승리 애니메이션
│   │   │   │   └── VideoBackground.jsx  # 비디오 배경
│   │   │   ├── hooks/
│   │   │   │   ├── usePcSocket.js       # PC Socket.io 훅
│   │   │   │   └── useBackgroundMusic.js
│   │   │   ├── store/
│   │   │   │   └── useGameStore.js      # Zustand 전역 상태
│   │   │   ├── assets/             # 이미지, 3D 모델, 사운드
│   │   │   │   ├── rod.glb              # 3D 낚시대 모델
│   │   │   │   ├── background_*.mp4     # 배경 영상
│   │   │   │   └── sounds/
│   │   │   ├── App.jsx
│   │   │   └── main.jsx
│   │   ├── Dockerfile
│   │   └── package.json
│   │
│   └── web-mobile/                 # 모바일 컨트롤러 웹앱
│       ├── src/
│       │   ├── views/
│       │   │   ├── LoginView.jsx        # 모바일 로그인
│       │   │   ├── ProfileSetupView.jsx # 프로필 설정
│       │   │   ├── LobbyView.jsx        # 대기실
│       │   │   ├── InGameView.jsx       # 게임 중 화면
│       │   │   └── ResultView.jsx       # 결과 화면
│       │   ├── hooks/
│       │   │   ├── useMobileSocket.js   # Mobile Socket.io 훅
│       │   │   ├── useAccelSensor.js    # 가속도 센서 훅
│       │   │   ├── useShake.js          # 흔들기 감지 훅
│       │   │   └── useWakeLock.js       # 화면 꺼짐 방지
│       │   ├── store/
│       │   │   └── useMobileStore.js    # Zustand 전역 상태
│       │   ├── App.jsx
│       │   └── main.jsx
│       ├── Dockerfile
│       └── package.json
│
├── infra/                          # 인프라 설정 파일
│   └── nginx/
│       ├── default.conf            # Nginx 설정
│       ├── default.conf.ssl        # SSL 설정
│       └── default.conf.init       # 초기 설정
│
├── docs/                           # 상세 문서
│   ├── API.md                      # API 명세서
│   ├── DATABASE.md                 # DB 스키마 설명
│   └── GAMEFLOW.md                 # 게임 플로우 기획서
│
├── docker-compose.yml              # 전체 서비스 오케스트레이션
├── README.md
└── package.json                    # 루트 패키지 (monorepo 관리)
```

---

## 🎯 주요 기술적 특징

### 1. **컴포넌트 기반 아키텍처 & 캡슐화**

#### Zustand를 활용한 상태 관리
- **완전한 캡슐화**: 각 클라이언트(PC, Mobile)는 독립적인 Zustand store 보유
- **Immer Middleware**: 불변성을 자동으로 관리하여 상태 업데이트 안전성 보장
- **Selector 패턴**: 필요한 상태만 구독하여 불필요한 리렌더링 방지

```javascript
// 예시: useGameStore.js
export const useGameStore = create(
  immer((set) => ({
    gameState: 'LOGIN',
    players: { A: [], B: [] },
    score: { A: 0, B: 0 },
    
    updateScore: (team, amount) =>
      set((draft) => {
        draft.score[team] += amount;  // Immer가 자동으로 불변 처리
      }),
  }))
);
```

#### 재사용 가능한 컴포넌트
- **PC 화면**: `PlayerCard`, `TeamPanel`, `QRCodePanel` 등 모듈화된 UI
- **3D 컴포넌트**: `FishingRod3D`, `CastingRod3D` - R3F 기반 독립 컴포넌트
- **모바일 화면**: View 단위 분리 (LoginView, LobbyView, InGameView)

### 2. **Custom Hooks를 통한 로직 재사용**

```javascript
// 센서 로직 분리
useAccelSensor()   // 가속도계 데이터 수집
useShake()         // 흔들기 감지 및 Schmitt Trigger
useWakeLock()      // 화면 꺼짐 방지

// 소켓 통신 분리
usePcSocket()      // PC 전용 소켓 이벤트 핸들링
useMobileSocket()  // Mobile 전용 소켓 이벤트 핸들링
```

### 3. **NestJS 모듈 시스템 활용**

- **모듈별 책임 분리**:
  - `AuthModule`: 인증 (Google OAuth, JWT)
  - `RoomsModule`: 방 관리 (REST API)
  - `GamesModule`: 게임 로직 (WebSocket Gateway)
  - `RedisModule`: Redis 클라이언트
  - `PrismaModule`: DB 연결

- **의존성 주입(DI)**: NestJS의 IoC 컨테이너를 통한 느슨한 결합

### 4. **실시간 센서 데이터 처리 최적화**

#### Schmitt Trigger 알고리즘
- 센서 노이즈 필터링
- 떨림 방지 (Debouncing)
- 정확한 흔들기 카운팅

```javascript
// useShake.js 핵심 로직
const SHAKE_THRESHOLD_HIGH = 15;  // 상위 임계값
const SHAKE_THRESHOLD_LOW = 8;    // 하위 임계값
const COOLDOWN_MS = 100;          // 쿨다운

if (!isShaking && magnitude > SHAKE_THRESHOLD_HIGH) {
  isShaking = true;
  onShake();  // 흔들기 이벤트 발생
}
if (isShaking && magnitude < SHAKE_THRESHOLD_LOW) {
  isShaking = false;  // 상태 리셋
}
```

### 5. **하이브리드 데이터베이스 전략**

| 상황 | PostgreSQL | Redis |
|------|------------|-------|
| 방 생성 | Room 레코드 저장 | `room:{id}` 초기화 |
| 플레이어 입장 | Player 레코드 저장 | `room:{id}:player:{pid}` 생성 |
| 게임 중 점수 | ❌ | `HINCRBY room:{id} score:A 1` |
| 게임 종료 | Game 결과 저장 | Redis 키 삭제 (TTL 30분) |

**장점**:
- PostgreSQL: 영구 데이터, 트랜잭션 보장
- Redis: 초고속 읽기/쓰기 (< 1ms), 원자적 연산

### 6. **Docker 기반 배포 자동화**

```yaml
# docker-compose.yml 구조
services:
  db:         # PostgreSQL
  redis:      # Redis
  backend:    # NestJS (depends_on: db, redis)
  web-mobile: # React 빌드 (output → volume)
  web-pc:     # React 빌드 (output → volume)
  nginx:      # Reverse Proxy (depends_on: backend, web-*)
  certbot:    # SSL 인증서 자동 갱신
```

**특징**:
- 멀티 스테이지 빌드로 이미지 크기 최적화
- Volume을 통한 빌드 결과물 공유
- Health Check를 통한 안전한 서비스 시작 순서 보장

### 7. **재연결 및 세션 복구 메커니즘**

- **Heartbeat**: PC Host는 30초마다 서버에 heartbeat 전송
- **Auto Reconnect**: Socket.io 내장 재연결 로직
- **State Sync**: 재연결 시 `room_state` 이벤트로 전체 상태 동기화
- **Redis 기반 세션**: 클라이언트 연결이 끊겨도 서버 상태 유지

---

## 🖼️ 화면별 기능 상세 설명

### 1️⃣ **LOGIN (로그인)**

#### PC 화면
- **그래픽**: 
  - 메인 배경 영상 (background_main.mp4) 전체 화면 재생
  - 반투명 글래스 패널 중앙 배치
  - Google 로그인 버튼 (브랜드 컬러 적용)
  
- **기능**:
  - Google OAuth 2.0 로그인
  - Access Token / Refresh Token 발급
  - 기존 활성 방 체크 (복구 가능 시 알림)

#### Mobile 화면
- **그래픽**:
  - 심플한 로그인 화면
  - Google 로그인 버튼
  - 방 코드 정보 표시 (QR 스캔 시)

- **기능**:
  - Google OAuth 로그인
  - URL 파라미터에서 방 코드 추출 (?code=ABC123)
  - 로그인 후 자동으로 ProfileSetup 또는 방 입장

---

### 2️⃣ **HOME (PC 전용: 방 생성)**

#### PC 화면
- **그래픽**:
  - 항구 배경 이미지
  - 방 생성 폼 (GlassPanel)
  - 팀 이름 입력 필드 (A팀, B팀)
  - 최대 인원 설정 슬라이더

- **기능**:
  - 방 생성 API 호출 (`POST /api/rooms`)
  - QR 코드 생성 (madcamp.cloud/?code=ABC123)
  - 자동으로 WebSocket 연결 및 WAITING 상태로 전환

---

### 3️⃣ **WAITING (대기실)**

#### PC 화면
- **그래픽**:
  - 항구 배경 (background_deck.png)
  - 좌우 팀 패널 (TeamPanel 컴포넌트)
    - 팀 이름 배지
    - 플레이어 카드 그리드 (프로필 이미지, 닉네임, 왕관 아이콘)
  - 중앙 QR 코드 패널 (QRCodePanel)
    - QR 코드 이미지
    - 방 코드 텍스트 (ABC123)
    - 접속 URL
  - 우측 상단: 호스트 프로필 및 로그아웃 버튼
  - 하단: 게임 시작 버튼 (모든 플레이어 Ready 시 활성화)

- **기능**:
  - 실시간 플레이어 입장/퇴장 감지
  - 플레이어별 Ready 상태 표시 (체크 아이콘)
  - 팀 이동 시 애니메이션 효과
  - 호스트: 게임 시작 버튼 (`start_cinematic` 이벤트)

#### Mobile 화면
- **그래픽**:
  - 상단: 방 이름 배지
  - 중앙: 내 프로필 카드
    - 프로필 이미지 (클릭 시 수정 가능)
    - 닉네임 (클릭 시 수정 가능)
    - 팀 배지 (A팀/B팀)
  - 팀 선택 버튼 (A팀, B팀)
  - 준비 완료 버튼 (토글)
  - 팀장: 팀장 위임 버튼

- **기능**:
  - 프로필 수정: 이미지 업로드, 닉네임 변경 (`POST /api/auth/profile`)
  - 팀 선택: `select_team` 이벤트 전송
  - 준비 완료: `toggle_ready` 이벤트 전송
  - 팀장 위임: `delegate_leader` 이벤트 전송

---

### 4️⃣ **CINEMATIC (시네마틱)**

#### PC 화면
- **그래픽**:
  - 전체 화면 시네마틱 영상 재생 (cinematic.mp4)
  - 페이드 인/아웃 전환 효과
  - 배가 출항하는 장면 (약 10초)

- **기능**:
  - 자동 재생 후 TUTORIAL 상태로 전환
  - BGM 재생 시작

#### Mobile 화면
- **그래픽**:
  - "출항 중입니다..." 텍스트
  - 로딩 애니메이션

- **기능**:
  - 햅틱 피드백 (배 출항 진동 패턴)
  - iOS WebKit에서는 햅틱 제외

---

### 5️⃣ **TUTORIAL (센서 확인 & 튜토리얼)**

#### PC 화면
- **그래픽**:
  - **Split Screen**: 화면이 좌우로 분할 (A팀 | B팀)
  - 선박 배경 (background_onship.png)
  - 각 팀:
    - 상단: 팀 이름 배지
    - 중앙: 플레이어 아바타 그리드
      - 프로필 이미지
      - 닉네임
      - 센서 확인 상태 (원형 테두리: 회색 → 초록색)
    - 하단: 안내 메시지
      - "휴대폰을 흔들어 센서를 확인합니다"
      - "아이폰은 권한을 허용해주세요"

- **기능**:
  - `sensor_checked` 이벤트 수신 시 해당 플레이어 아바타 테두리 초록색 전환
  - 모든 플레이어 센서 확인 완료 시:
    - 효과음 재생
    - 튜토리얼 메시지로 전환
      - "Casting 후 Hit이 발생하면, 휴대폰을 흔들어 게이지바를 채워서 물고기를 낚아주세요!"
    - 3초 후 자동으로 CASTING 상태로 전환

#### Mobile 화면
- **그래픽**:
  - 상단: "센서 확인" 배지
  - 중앙: 큰 흔들기 아이콘 (애니메이션)
  - 하단: 
    - "휴대폰을 흔들어주세요!"
    - iOS: "권한 허용 후 흔들어주세요" (requestPermission 버튼)

- **기능**:
  - **센서 확인**:
    - `useShake` 훅으로 흔들기 감지
    - 첫 번째 흔들기 감지 시 `sensor_checked` 이벤트 전송
    - 배경색 일시적으로 초록색 플래시
  - **튜토리얼 메시지 표시**:
    - PC와 동기화된 안내 문구 표시

---

### 6️⃣ **CASTING (캐스팅)**

#### PC 화면
- **그래픽**:
  - **Phase 1: 캐스팅 준비 (선박 뷰)**
    - Split Screen 유지
    - 각 팀 화면에 카운트다운 표시 (5 → 1)
    - "Casting을 준비해주세요!" 메시지
    - 팀장 프로필 강조 (빛나는 효과)
  
  - **Phase 2: 캐스팅 실행 (바다 뷰)**
    - 3D 낚시대 애니메이션
    - 찌(Float)가 날아가는 모션 블러 효과
    - Power 값에 따라 거리 표시 (0m ~ 100m)
    - 물에 착수 시 물보라 효과 (WaterSplash)
  
  - **Phase 3: 입질 대기 (선박 뷰)**
    - 선박 배경으로 복귀
    - "입질을 기다리는 중..." 메시지
    - 고정 시간(5초) 후 찌가 물속으로 잠김
    - "HIT!" 텍스트 효과

- **기능**:
  - 호스트: `start_casting` 이벤트 전송 (자동 또는 수동)
  - 서버에서 카운트다운 시작 (`casting_countdown` 이벤트)
  - 카운트다운 0 도달 시 캐스팅 윈도우 오픈 (`casting_start` 이벤트)
  - 팀장: `cast_action` 이벤트 수신 → Power 값 표시
  - 팀장: `cast_complete` 이벤트 수신 → 캐스팅 완료 애니메이션
  - 양 팀 모두 캐스팅 완료 시 PLAYING 상태로 자동 전환

#### Mobile 화면
- **그래픽**:
  - **팀장**:
    - 상단: "Casting을 준비하세요!" 배지
    - 중앙: 카운트다운 표시 (5 → 1)
    - 하단: 캐스팅 가이드 이미지 (폰을 휘두르는 모션)
    - 캐스팅 윈도우 오픈 후:
      - "지금 던지세요!" (펄스 애니메이션)
      - Power 게이지 (0 ~ 100)
  
  - **일반 팀원**:
    - "팀장이 Casting 중입니다..." 메시지
    - 대기 애니메이션

- **기능**:
  - **팀장**:
    - 카운트다운 종료 후 2초 동안 캐스팅 윈도우
    - `useAccelSensor` 훅으로 가속도 측정
    - Peak 감지 시 `cast_action` 이벤트 전송 (power: 0~100)
    - 2초 후 자동으로 `cast_complete` 전송
  - **일반 팀원**:
    - 대기 상태 유지
    - 서버 이벤트 수신 대기

---

### 7️⃣ **PLAYING (메인 게임)**

#### PC 화면
- **그래픽**:
  - **Split Screen**: A팀 | B팀
  - 각 팀 화면:
    - **3D 낚시대** (FishingRod3D):
      - SVG Path Morphing으로 텐션 표현
      - 점수 증가 속도(Velocity)에 따라 휘어짐 정도 변화
    - **거리 게이지**:
      - 프로그레스 바 (100m → 0m)
      - 현재 거리 텍스트
    - **물고기 애니메이션**:
      - Lerp(선형 보간)로 부드러운 이동
      - 점수에 따라 실시간 위치 업데이트
    - **Recent Shakers**:
      - 최근 흔든 플레이어 아바타 큐 (최대 10명)
      - 왼쪽에서 들어와 오른쪽으로 빠짐 (Queue 동작)
    - **팀 점수 표시**:
      - 상단 중앙에 큰 폰트로 점수

- **기능**:
  - `score_update` 이벤트 수신:
    - 팀 점수 증가
    - 개인 점수 증가
    - Recent Shakers 업데이트
    - 물고기 위치 업데이트
  - Shake History 기반 낚시대 텐션 계산
  - 목표 점수(또는 거리 0m) 도달 시 게임 종료
  - 승리 팀 결정 → `game_ended` 이벤트 수신

#### Mobile 화면
- **그래픽**:
  - 상단: 현재 게임 상태 "게임 진행 중!" 배지
  - 중앙: 큰 흔들기 아이콘 (Lucide Waves)
  - 하단: 
    - "열심히 흔들어주세요!" 메시지
    - 내 기여 점수 표시 (개인 카운터)

- **기능**:
  - `useShake` 훅으로 지속적으로 흔들기 감지
  - 흔들기 감지 시:
    - 배경색 초록색 플래시 (100ms)
    - `shake` 이벤트 전송 (count: 1)
    - 로컬 카운터 증가
  - Schmitt Trigger로 노이즈 필터링
  - 쿨다운 (100ms) 적용으로 과도한 전송 방지

---

### 8️⃣ **FINISHED (결과 화면)**

#### PC 화면
- **그래픽**:
  - **Phase 1: 게임 종료 애니메이션**
    - 물고기가 수면 위로 올라오는 애니메이션
    - 낚은 아이템 이미지 표시 (fishingItems.js 기반)
    - Confetti 애니메이션 (승리 팀 색상)
  
  - **Phase 2: 결과 모달**
    - 항구 배경으로 전환
    - 중앙 대형 모달 (GlassPanel):
      - 승리 팀 트로피 아이콘
      - "🏆 A팀 승리!" 타이틀
      - 팀별 최종 점수 (A: 1000 vs B: 950)
      - **리더보드 테이블**:
        - 순위, 프로필, 닉네임, 팀, 기여 점수
        - 1등 MVP 황금색 강조
      - 하단: "결과 저장 및 나가기" 버튼

- **기능**:
  - `game_ended` 이벤트 수신:
    - 승리 팀 정보
    - 플레이어별 최종 점수
    - MVP 정보
  - "결과 저장 및 나가기" 버튼 클릭 시:
    - PostgreSQL에 Game 레코드 저장
    - Redis 게임 세션 삭제
    - 방 상태 FINISHED로 업데이트
    - HOME 화면으로 리다이렉트

#### Mobile 화면
- **그래픽**:
  - 상단: 승리 팀 배지 (황금색)
  - 중앙: 
    - 결과 요약 카드
    - 내 순위 및 점수 (강조)
    - 내 팀 순위
  - 하단: "나가기" 버튼

- **기능**:
  - `game_ended` 이벤트 수신 → 결과 데이터 저장
  - 내 정보 하이라이트
  - "나가기" 버튼 → 앱 종료 또는 로그인 화면으로

---

## 🎨 시스템 아키텍처 이미지 제작 프롬프트

```
시스템 아키텍처 다이어그램 생성 요청

주제: KaHook! 인터랙티브 멀티스크린 파티 게임 시스템 아키텍처

구성 요소:
1. 클라이언트 계층 (상단):
   - 왼쪽: PC Host (💻 아이콘, "React + React Three Fiber + Zustand" 레이블)
   - 오른쪽: Mobile Players (📱 아이콘, "React + DeviceMotion API + Zustand" 레이블)
   - 화살표: 양방향 통신 (WebSocket + REST API 레이블)

2. 서버 계층 (중앙, AWS EC2 클라우드 박스 안):
   - 상단: Nginx 레이어 (🌐 아이콘)
     - "SSL Termination (Let's Encrypt)" 레이블
     - "Reverse Proxy" 레이블
     - "Static File Serving" 레이블
   
   - 중앙: NestJS Backend (⚙️ 아이콘)
     - 좌측: REST API 모듈
       - /api/auth (인증)
       - /api/rooms (방 관리)
     - 우측: WebSocket Gateway
       - /game (실시간 통신)
       - Socket.io 이벤트 처리
   
   - 하단: 데이터베이스 레이어
     - 왼쪽: PostgreSQL (🔵 아이콘)
       - "영구 데이터" 레이블
       - "사용자/방/게임 정보" 레이블
     - 오른쪽: Redis (🔴 아이콘)
       - "실시간 상태" 레이블
       - "게임 세션/점수" 레이블

3. 통신 흐름:
   - 클라이언트 → Nginx: HTTPS/WSS (443포트)
   - Nginx → NestJS: HTTP/WS (3000포트)
   - NestJS → PostgreSQL: Prisma ORM
   - NestJS → Redis: ioredis 클라이언트

4. 특별 강조 요소:
   - Redis와 WebSocket Gateway 사이에 굵은 화살표 (실시간 점수 집계 강조)
   - Mobile → Server → PC 경로를 하이라이트 (shake 이벤트 플로우)

스타일 요구사항:
- 깔끔한 다이어그램 스타일 (Lucidchart, Draw.io 스타일)
- 컬러 스킴: 
  - PostgreSQL: 파란색 계열
  - Redis: 빨간색 계열
  - Nginx: 초록색 계열
  - NestJS: 빨간색 계열 (NestJS 브랜드 컬러)
  - 클라이언트: 회색 계열
- 아이콘 사용: 이모지 또는 Font Awesome 스타일
- 박스: 둥근 모서리, 그림자 효과
- 화살표: 명확한 방향성, 레이블 포함

출력 형식: PNG, 4K 해상도, 투명 배경
```

---

## 📜 라이선스 및 저작권

이 프로젝트는 **MIT 라이선스**를 따릅니다. 단, 프로젝트에 포함된 일부 3D 자산은 별도의 라이선스를 따릅니다.

### 3D Assets Attribution

본 프로젝트에 사용된 낚시대 3D 모델(`.glb`)은 **Sketchfab**의 **Francesco Coldesina**님이 제작하였으며, **CC-BY 4.0** 라이선스 하에 사용되었습니다.

- **Asset Name:** Fishing Rod
- **Author:** Francesco Coldesina
- **Source:** [Sketchfab Link](https://sketchfab.com/3d-models/canna-da-pesca-f8031fbbea9a4a9fbd3ab23ee6334c00)
- **License:** CC-BY 4.0 (Creative Commons Attribution)

---

## 💡 개발 후기

### 안준영
*(후기 작성 대기)*

---

### 임태빈
*(후기 작성 대기)*

---

## 🚀 향후 개선 방향

1. **멀티 게임 모드**: 
   - 시간 제한 모드
   - 특정 물고기 잡기 미션 모드
   - 개인전 모드

2. **전적 시스템**:
   - 개인 통계 페이지
   - 글로벌 리더보드
   - 업적 시스템

3. **소셜 기능**:
   - 친구 초대 시스템
   - 리플레이 공유
   - 하이라이트 클립 생성

4. **접근성 개선**:
   - 시각/청각 장애인을 위한 대체 UI
   - 다국어 지원 (i18n)
   - 더 넓은 기기 호환성

5. **성능 최적화**:
   - WebWorker를 통한 센서 처리 오프로딩
   - 3D 모델 LOD (Level of Detail) 시스템
   - 네트워크 대역폭 최적화 (Binary Protocol)

---

## 📞 문의

프로젝트에 대한 문의사항이나 버그 제보는 다음 채널로 연락 주세요:

- **GitHub Issues**: [Repository Issues](https://github.com/your-repo/mad-together/issues)
- **Email**: contact@madcamp.cloud

---

**Made with ❤️ by 안준영 & 임태빈**
