{
  "name": "fish-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "server:dev": "yarn workspace @fish/server dev",
    "pc:dev": "yarn workspace @fish/web-pc dev",
    "mobile:dev": "yarn workspace @fish/web-mobile dev",
    "build": "yarn workspaces run build"
  }
}
